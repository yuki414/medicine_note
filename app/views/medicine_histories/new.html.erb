<% provide(:title, 'edit') %>
<h1>お薬情報の追加</h1>
<div id="center_Box">
  <%= render 'shared/medicineh_image' %>
</div>
<div class="col-md-6 col-md-offset-3">
  <div class = "h3">
    お薬名：<%= Medicine.find(@mhis.medicine_id).name %>
  </div>
  
  <div class = "h4">
    お薬の説明：<%= Medicine.find(@mhis.medicine_id).description %>
  </div>
</div>
<div class="row">
  <aside class="col-md-8 col-md-offset-2">
    <%= form_for(@mhis) do |f| %>
      
      <%= f.label :date, "日付" %>
      <%= f.date_field :date %>

      <%= f.label :pharmacist_id, "薬剤師名" %>
      <%= f.collection_select :pharmacist_id, Pharmacist.all, :id, :name %>

      <%= f.hidden_field :medicine_id, :value => @mhis.medicine_id %>

      <%= f.label :adm, "用法" %>
      <%= f.text_field :adm %>

      <%= f.label :dose, "用量" %>
      <%= f.text_field :dose %>

      <%= f.submit "追加", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>