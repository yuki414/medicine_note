<% provide(:title, @medicines.length) %>
<% @medicines.each do |med| %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
          <%= med.name %>,<%= med.effect %>
      </section>
    </aside>
  </div>
<% end %>